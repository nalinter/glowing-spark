pipeline{
agent none
environment{
    pr_number = "2019"
    }
stages{
    
    stage('hello'){
        steps{
                input id: 'APPROVAL', message: 'DEPLOY PACKAGE TO SBX - APPROVAL', ok: 'APPROVE', submitter: 'nalinter@in.ibm.com'
                echo "${JOB_URL}"
        }
    }   
    
}
}
